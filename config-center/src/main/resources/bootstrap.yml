spring:
  application:
    name: config-center

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bmd007/MicroservicesPlayGround
          clone-on-start: false
          search-paths:
            - configurations
          basedir:
            ../tmp
