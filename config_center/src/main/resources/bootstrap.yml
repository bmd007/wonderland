spring:
  application:
    name: config_center

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bmd007/MicroservicesPlayGround
          clone-on-start: true
          search-paths:
            - configurations
          basedir:
            ../tmp
