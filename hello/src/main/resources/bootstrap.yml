spring:
  application:
    name: hello
#the line below is not needed because http://localhost:8888 is the default value but will be kept for sake of clarity
#in deployment environments, it should be defined as a ENV_VAR
  cloud.config.uri: http://localhost:8888
